import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['images.unsplash.com', 'encrypted-tbn0.gstatic.com', 'getproofed.com.au'],
  },
};

export default nextConfig;
